import Router from "./shares/Router";
import { Reset } from "styled-reset";
import React from "react";
const App = () => (
  <React.Fragment>
    <Reset />
    <Router />
  </React.Fragment>
);

export default App;
